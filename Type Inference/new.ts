const calculateArea = (length, width) => {
    return length * width
};

let total = calculateArea(24, 42);

console.log(`The area of a rectangle with length 24 inches and width 42 inches is ${total} square inches.`)